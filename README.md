# Jewbot - A chatbot used mostly for internal Beit Hatfutsot tasks / devops

## Quickstart

* `sudo apt-get install python3 python3-dev`
* `pip3 install -r requirements.txt`
* `mkdir data`
* `errbot -T`

## Configuration

* `cp .env{.example,}`
* edit .env
* restart errbot
